1. ~~when removing a game from the game list in create game part2, the checkmark stays active and the game cannot be readded. after removing a game from the list, it should get available again.~~ ✅
2. ~~hosts should also be able to add games from the library during an active olympics~~ ✅
3. After creating an olypics, user should have the option to save as draft or immidiatly launch the oly -< isnt resolved yet. there is still just the save button
4. ~~while in a game, the navbar is inaccessable. users should still interact with other parts of the website. there should be an option (maybe a little popup in the corner) where users can join back to the lobby.~~ ✅ (pill on room pages + global rejoin pill on non-room pages when a room is active in localStorage)
5. ~~there is a bug, where the host sees himself twice in the lobby when choosing "Host spielt mit" -> please review setup here~~ ✅
6. ~~there is a bug, where the host selects "Spiele verstecken", the games are not actually hidden and in the admin panel the setting is setr to disabled. when enabling, the feature works. so it doesnt get set properly~~ ✅ (PATCH route was silently dropping hideGamePlan when editing a draft)
7. ~~people can join the game after the waiting room, these players are not getting added to the leadboard then. we need a strikter lobby, where people cant join running olypmics if they werent in the waiting lobby. we need an extra page for that.~~ ✅ (server rejects late joiners with GAME_IN_PROGRESS error; client shows a dedicated blocked page)
8. I changed a bit on the user dropdown. i wanted a point called "Drafts" that opens a new page, listing all current drafts, not the drafts inside the dropdown. please adjust. -> the option is now there, but the button just brings me to the homepage. please add a page for drafts. ✅
9. Drafts lassen sich aktuell nicht starten. Sie lassen sich nur speichern. auch von der draft seite komme ich nur in den edit zurück und bin somit in einem endlos loop gefangen.
